version: '3.9'

services:

  mongo-topapi:
    image: mongo
    container_name: mongo-topapi
    restart:
      always
    ports:
      - 27017:27017
    volumes:
      - ./.mongo:/data/db
    networks:
      - topapi

networks:
  topapi: