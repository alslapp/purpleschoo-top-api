version: '3.9'

services:
  mongo:
    image: mongo
    container_name: mongo
    restart:
        always
    ports:
      - 27017:27017
    volumes:
      - ./mongo-data:/data/mongo
    networks:
      - bridge-topapi

networks:
  bridge-topapi:
    driver: bridge