version: '3.1'

services:
  mongo:
    image: mongo
    container_name: mongo
    restart:
        always
    ports:
      - 27017:27017
    volumes:
      - ./mongo-data:/data/db
    networks:
      - bridge-topapi

networks:
  bridge-topapi:
    driver: bridge