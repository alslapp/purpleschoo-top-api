version: '3.1'

services:
  top-api:
    image: top-api:latest
    container_name: top-api
    restart: always
    ports:
      - 3330:3330
    volumes:
      - ./.env:/opt/app/.env
    networks:
      - bridge-topapi

networks:
  bridge-topapi:
    driver: bridge